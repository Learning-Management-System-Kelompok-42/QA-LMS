name: CI API Automation Testing with Postman using Newman

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x, 14.x, 16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        testFilePath: api-automation/
    - run: npm ci
    - run: npx newman run ../../api-automation/Capstone\ Refactoring.postman_collection.json -e API-LMS-42.postman_environment.json
    - run: npm test
